{{!--
  Plugin outlet: discovery-list-container-top
  Mount point for the Gasing Circle Community Dashboard.
  The actual DOM is injected via JS for full control over layout & data fetching.
  The default topic list below this outlet is hidden via SCSS when body.category-general is active.
--}}
<div class="gc-community-wrapper" id="gc-community-layout" data-component="gasing-circle-community-layout"></div>
